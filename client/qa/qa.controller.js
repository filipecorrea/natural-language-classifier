angular.module('natural-language-classifier').controller('qaController', function ($scope, $rootScope) {

  $scope.textChanged = function(event) {
    if ($scope.text) {

      // TODO: Call NLC
    }
  };

});
